/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  compiler: {
    // Support for SSR, styles minification, debugging experience.
    styledComponents: true,
  },
}

export default nextConfig
